import click from 'sounds/click.wav'
import immovableTile from 'sounds/immovable-tile.mp3'
import lid from 'sounds/lid.mp3'
import reset from 'sounds/reset.mp3'
import settings from 'sounds/settings.mp3'
import shuffle from 'sounds/shuffle.mp3'
import tile from 'sounds/tile.mp3'
import warning from 'sounds/warning.mp3'
import warningClose from 'sounds/warningClose.wav'


export const clickSound = new Audio(click)

export const immovableTileSound = new Audio(immovableTile)

export const lidSound = new Audio(lid)

export const resetSound = new Audio(reset)

export const settingsSound = new Audio(settings)

export const shuffleSound = new Audio(shuffle)

export const tileSound = new Audio(tile)

export const warningSound = new Audio(warning)

export const warningCloseSound = new Audio(warningClose)

